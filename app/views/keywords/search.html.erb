<div>
  <span>Total match URLs: <%= search_result_presenter.total_urls %></span>
  <% search_result_presenter.search_result.each do |item| %>
    <div>
      <span>Keyword ID: <%= item[:keyword_id] %></span>
      <ul>
        <% item[:urls].each do |item| %>
          <li><%= item %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>
